import expect from 'expect';
import rootReducer from './rootReducer';

describe('root reducer', () => {

  test('load root reducer success', () => {
    const rootreducer = rootReducer;
    expect(true).toBe(true);
  });


});
